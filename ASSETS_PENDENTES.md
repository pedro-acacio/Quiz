# Assets pendentes

Este projeto foi extraído do zip `PRO-V2-C36-Modelo-Projeto-main.zip` (mantido no repositório por enquanto). O código-fonte já está aqui.

Falta subir manualmente:

- `p5.play.js` (biblioteca — as demais libs já vêm de CDN externo no index.html)

Baixe o arquivo `assets-quiz.zip` enviado no chat e arraste o arquivo para esta branch pela interface do GitHub ("Add file" → "Upload files"). Depois disso, pode apagar o .zip original do template.

**Importante sobre este projeto:** mesmo com tudo no lugar, o quiz não funciona de verdade — é o template original do curso, nunca preenchido. Em `js/Question.js`, toda a lógica de verificar a resposta e mostrar feedback está comentada (dentro de `button.mousePressed`), e `js/Contestant.js` está vazio. Clicar em "Enviar" não faz nada.
